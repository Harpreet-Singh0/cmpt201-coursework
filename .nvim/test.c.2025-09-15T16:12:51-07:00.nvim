#include <stdio.h>
#include <unistd.h>
#include<sys/types.h>
int main() {
  int pid;
  pid = fork();
  if(pid == 0){
    printf("CHILD: PID=%d, parent=%d",pid,getppid());
  }
  else{
    printf("PARENT: PID=%d, child=%d",pid,getpid());
  }
    return 0;

}
